<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!--Instead of writing own CSS, Bootstrap is imported and used-->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <title>Document</title>
</head>

<body>
    <div class="container">
        <h1>URL Shortener</h1>
        <!--This form gets rendered first-->
        <!--We are going to post to the /shorturls endpoint by creating a post in server.js-->
        <form action="/shorturls" method="POST" class="my-4 form-inline">
            <label for="fullUrl" class="sr-only">URL</label>
            <input type="url" name="fullUrl" id="fullUrl" class="form-control col mr-2">
            <button class="btn btn-success" type="submit">Shorten</button>
        </form>

        <!--This table wil contain all the different results for the shorturls that are already generated-->
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Complete URL</th>
                    <th>Shortened URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <!--Looping through every single short URL-->
                <% shorturls.forEach(shorturl => { %>
                <tr>
                    <td><a href="<%= shorturl.full %>"><%= shorturl.full %></a></td>
                    <td><a href="<%= shorturl.short %>"><%= shorturl.short %></a></td>
                    <td><%= shorturl.clicks %></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>